# Blog Service

Микросервис для управления блогом с авторизацией, созданием и редактированием постов.

## 📝 Описание

Проект реализует бэкенд-сервис для блога на Go. Основные возможности:

- **Авторизация и аутентификация** через JWT токены
- **CRUD операции** для постов
- ️**Загрузка изображений** к постам
- **Ролевая модель доступа** (авторы, читатели)
- **Хранение файлов** в хранилище MinIO
- **Автоматическая документация** API через Swagger

## 🛠 Технологический стек

### Бэкенд
- **Язык программирования:** Go 1.21+
- **База данных:** PostgreSQL 15+
- **Файловое хранилище:** MinIO (S3-совместимое)
- **Документация API:** Swagger/OpenAPI

### Инфраструктура
- **Контейнеризация:** Docker + Docker Compose
- **Миграции БД:** golang-migrate
- **Логирование:** Uber Zap

## 📋 Предварительные требования

### Для локальной разработки
- Go 1.21 или выше
- Git
- Swag (для генерации документации)

### Для запуска через Docker
- Docker 20.10+
- Docker Compose 2.0+

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/kr0uch/blog.git
cd blog
```

### 2. Настройка окружения
Создайте файл `.env` на основе примера:

```env
# Конфигурация PostgreSQL
POSTGRES_PORT=5432
POSTGRES_HOST=localhost           # Используйте 'postgres' для запуска в Docker
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123
POSTGRES_SSLMODE=disable
POSTGRES_DB=microblog

# Конфигурация MinIO
MINIO_ENDPOINT=localhost:9000     # Используйте 'minio:9000' для Docker
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=miniopassword
MINIO_USE_SSL=false

# Конфигурация приложения
PORT=8080
SECRET=secret                     # Секретный ключ для JWT
BUCKET=data                       # Название бакета в MinIO
```
Отредактируйте `.env` файл, указав необходимые настройки.

**Важно:** При запуске через Docker Compose замените `localhost` на имена сервисов (`postgres` и `minio` соответственно).


### 3. Запуск через Docker (рекомендуется)
```bash
# Сборка и запуск всех сервисов
docker-compose up --build -d
```

### 4. Запуск локально (для разработки)
```bash
# Установка зависимостей Go
go mod download

# Запуск приложения
go run cmd/main.go
```

## 📖 Документация API

После запуска сервиса документация доступна по адресу:
- **Swagger UI:** http://localhost:8080/swagger/
- **OpenAPI спецификация:** http://localhost:8080/swagger/doc.json

### Обновление документации
Если вы вносите изменения в API, обновите документацию:
```bash
# Установите swag, если еще не установлен
go install github.com/swaggo/swag/cmd/swag@latest

# Генерация документации
swag init -g cmd/main.go -o docs
```

## 🧪 Тестирование

### Запуск модульных тестов
```bash
# Тестирование контроллеров
go test ./internal/transport/rest/controllers/... -v
```

## 🐳 Docker команды

### Управление сервисами
```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Пересборка и перезапуск
docker-compose up --build -d
```

### Доступ к сервисам
- **Приложение:** http://localhost:8080
- **MinIO Console:** http://localhost:9001 (логин: minioadmin/miniopassword)
- **PostgreSQL:** localhost:5432